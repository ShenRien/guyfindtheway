<style>
  * {
    padding: 0;
    margin: 0;
    line-height: calc(1em + 0.75rem);
  }
  
  html {
    --color-background: #fffcf0;
    --color-background-shade: #fbf3d9;
    --color-text: #22211d;
    --color-text-title: #eee6d7ff;
    --color-text-shade: #575650;
    --color-primary: #147d82;
    --color-post: #f37d5c10;
    --color-post2: #f4d2c920;

    /* --font-family: -apple-system, BlinkMacSystemFont, "Inter", "IBM Plex Sans",
      Segoe UI, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji,
      Segoe UI Symbol;
    */
    

    --container-max-size: 75ch;
    --container-padding-sides: var(--space-2);

    --space-1: 0.35rem;
    --space-2: 0.5rem;
    --space-3: 1rem;
    --space-4: 1.5rem;
    --space-5: 2rem;
    --space-6: 2.5rem;
    --space-7: 3rem;
    --space-8: 4rem;
    --space-9: 5rem;
    --space-10: 6rem;

    --font-size-xs: clamp(0.8rem, 0.85vw + 0.8rem, 1rem); /* chú thích nhỏ */
    --font-size-base: clamp(0.9rem, 0.8vw + 0.9rem, 1.45rem); /* văn bản chính */
    --font-size-lg: clamp(1.2rem, 0.8vw + 1.2rem, 1.75rem); /* tiêu đề nhỏ */
    --font-size-xl: clamp(1.6rem, 0.8vw + 1.6rem, 2.5rem); /* tiêu đề lớn */
  }
  [data-theme="dark"] {
    --color-background: #100f0f;
    --color-background-shade: #202424;
    --color-text: #d2dee1;
    --color-text-title: #d2dee1;
    --color-text-shade: #989f9f;
    --color-primary: #62aba4;
    --color-post: #9a817a2d;
  }
  h1 {
    font-size: var(--font-size-xl);
  }
  h2 {
    font-size: var(--font-size-lg);
    color: var(--color-text-shade);
    font-weight: normal;
    margin-bottom: var(--space-2);
  }
  h3,
  h4,
  h5,
  h6 {
    font-size: var(--font-size-base);
  }
  body {
    font-family: var(--font-family) !important;
    color: var(--color-text);
    background-color: var(--color-background);
    font-size: 1rem;
    font-size: var(--font-size-base);
    text-rendering: optimizeLegibility;
  }
  a {
    color: var(--color-primary);
  }
  img,
  picture {
    height: auto;
    max-width: 100%;
    flex-shrink: 0;
  }
  blockquote {
    padding: 0 1em;
    color: var(--color-text-shade);
    border-left: 0.25em solid var(--color-primary);
  }
  button {
    display: inline;
    background-color: var(--color-primary);
    color: white;
    border-radius: 4px;
    border: none;
    font-size: var(--font-size-base);
    padding: 0.5em 1em;
    font-size: inherit;
    line-height: 1.15;
    transition: background 0.2s ease-in-out;
    cursor: pointer;
  }
  button:hover {
    background-color: var(--color-text);
    color: white;
  }
  pre {
    padding: var(--space-4);
    border-radius: 4px;
    font-size: var(--font-size-xs);
    background: var(--color-background-shade);
    --prism-background: var(--color-background-shade);
    --prism-font-size: var(--font-size-xs);
  }
  code {
    font-size: var(--font-size-xs);
    white-space: pre-wrap;
    word-spacing: normal;
    word-break: normal;
    word-wrap: normal;
    -moz-tab-size: 4;
    -o-tab-size: 4;
    tab-size: 4;

    -webkit-hyphens: none;
    -moz-hyphens: none;
    -ms-hyphens: none;
    hyphens: none;
  }

  .visually-hidden {
    clip: rect(0 0 0 0);
    clip-path: inset(50%);
    height: 1px;
    overflow: hidden;
    position: absolute;
    white-space: nowrap;
    width: 1px;
  }



:root {
    --font-family: "CharisSIL_Local", "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
  }

html{
    font-family: var(--font-family), "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
      
}

@font-face {
  font-family: "CharisSIL_Local"; 
  src: url("{{ '/assets/fonts/CharisSIL-Regular.ttf' | url }}") format("truetype");
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}

.article {
    border: 0 !important;
    border-radius: 0 !important;
}

.emoji-fix {
  font-family: -apple-system, BlinkMacSystemFont, "Inter", "IBM Plex Sans",
      Segoe UI, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji,
      Segoe UI Symbol !important; /* Buộc trình duyệt thoát khỏi CharisSIL */
}

</style>

<style>


/*
8888888888888888888888888888888
Postlist
8888888888888888888888888888888
*.


  /* reset ul list style */
  ul {
    list-style: none;
  }

  /* image vuông fit */
  .article-image {
    position: relative;
    width: 100%;
    padding-bottom: 100%; /* Tạo khung vuông */
    overflow: hidden;
    border-radius: 3px;
  }

  .article-image img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .article-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(30%, 1fr));
    grid-auto-rows: auto;
    gap: var(--space-1);
    list-style: none;
    padding: 0;
  }

  @media (max-width: 768px) {
    .article-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .Newest {
    display: flex;
    flex: 1 1 100%;
    min-width: 100%;
    min-height: var(--space-2);
    border: .2px solid var(--color-border);
    border-radius: 5px;
    transition: all 0.3s ease;
    box-sizing: border-box;
  }

    .article-image-newest {
    position: relative;
    width: 30%;
    padding-bottom: 30%; /* Tạo khung vuông */
    overflow: hidden;
    border-radius: 3px;
    float: left;
    display: flex;
  }
  .article-image-newest img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
  }

  .articleNewest {
    /*opacity: 0.2;*/
    position: relative;
    gap: var(--space-2);
    align-items: center;
    justify-content: center;
    border-radius: 15px;
  }

  .article-content-newest {
    margin-left: 32%;
    display: block;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    margin-top: 0;
  }

  .articleNewest * {
    text-decoration: none !important;
  }

 /* responsive cho newest */
 @media (max-width: 768px) {
  .articleNewest {
    display: block;
    flex-direction: column;
    height: auto;
    text-align: left;
    position: none;
  }
  
  .article-image-newest {
    display: block;
    width: 100%;
    height: 300px;
  }
  
  .article-content-newest {
    margin-left: 0;
    margin-top: var(--space-4);
    padding: 0 var(--space-2);
    display: inline-block;
    width: 100%;
    border-radius: 5px;
    position: static;
    transform: none;
  }
}



  .article {
    /*background-image: linear-gradient(to bottom right, var(--color-post2), var(--color-post));*/
    /*opacity: 0.2;*/
    padding: var(--space-1);
    min-height: var(--space-2);
    border: .2px solid var(--color-border);
    border-radius: 5px;
    transition: all 0.3s ease;
    box-sizing: border-box;
    background: rgba(255, 255, 255, 0.2);
    box-shadow: 0 4px 5px rgba(0, 0, 0, 0.1);
    backdrop-filter: blur(6.3px);
    -webkit-backdrop-filter: blur(6.3px);
    border: 1px solid rgba(255, 255, 255, 0.4);

  }

  .article:hover{
    color: var(--color-post);
  }



  
  .article a {
    text-decoration: unset;
    display: block;
    height: 100%;
  }
  
  .article + .article {
    margin-top: 0;
    margin-bottom: 0;
  }
  
  .article-title {
    color: var(--color-text);
    transition: color 0.2s ease-in-out;
    margin-bottom: var(--space-2);
    font-size: var(--font-size-sm);
    line-height: 1.4;
    overflow: hidden;
   display: -webkit-box;
   -webkit-box-orient: vertical;
    line-height: 1.4;
  }
  
  .article:hover .article-title {
    color: var(--color-subtext);
  }

  
  
  .article-description {
    font-size: var(--font-size-xs);
    color: var(--color-text-shade);
    line-height: 1.5;
    overflow: hidden;
   display: -webkit-box;
   -webkit-line-clamp: 3;
           line-clamp: 3; 
   -webkit-box-orient: vertical;
  }
  
  .article-time {
    color: var(--color-text-shade);
    font-size: calc(var(--font-size-xs)* 0.8);
    margin-bottom: var(--space-2);
    overflow: hidden;
   display: -webkit-box;
   -webkit-line-clamp: 2;
           line-clamp: 2; 
   -webkit-box-orient: vertical;
   line-height: 1.4;
    height: calc(1 * 1.4em);
    min-height: calc(1 * 1.4em);
  }

  .article-content {
    decoration: none;
    font-size: calc(var(--font-size-xs) * 1.1);
    color: var(--color-text-shade);
    line-height: 1.5;
    overflow: hidden;
   display: -webkit-box;
   -webkit-line-clamp: 4;
           line-clamp: 4; 
   -webkit-box-orient: vertical;
   
  }

  

  /* được dùng ban đầu và có thể dùng khi có nhiều bài viết, nhưng có bug layout
  .article-grid {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-1);
    list-style: none;
    padding: 0;
     ĐÃ XÓA: margin-bottom: -1.5em;
  }
  */
  .article {
    background-color: var(--color-post);
    flex: 1 1 calc(33.33% - var(--space-1));
    min-width: 100px;
    padding: var(--space-3);
    min-height: var(--space-2);
    border: .2px solid var(--color-border);
    border-radius: 0px;
    transition: all 0.3s ease;
    box-sizing: border-box;
  }

  .articleNewest {
    flex: 1 1 calc(33.33% - var(--space-1));
    min-width: 100px;
    padding: var(--space-1);
    min-height: var(--space-2);
    border: .2px solid var(--color-border);
    border-radius: 0px;
    transition: all 0.3s ease;
    box-sizing: border-box;
  }
  
 
</style>
